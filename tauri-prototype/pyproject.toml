[tool.uv]
dev-dependencies = []

[project]
name = "speerium-backend"
version = "0.1.0"
description = "Speerium Python Backend with PyTauri"
requires-python = ">=3.9"
dependencies = [
    "pytauri>=0.8.0",
    "pydantic>=2.0.0",
]
