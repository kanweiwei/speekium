# 🚀 如何启动 Speekium Tauri 应用

**更新**: 2026-01-09
**状态**: 架构重构已完成，准备启动

---

## ⚠️ 启动前准备

### 1. 重新安装依赖（必须）

由于 Tauri CLI 原生模块需要重新安装，请执行：

```bash
cd tauri-prototype
rm -rf node_modules package-lock.json
npm install
```

这将下载所有必要的依赖，包括平台特定的原生模块。

---

## ✅ 启动应用

### 方法 1: 标准启动（推荐）

```bash
cd tauri-prototype
npm run tauri dev
```

**预期结果**:
- Vite 开发服务器启动（http://localhost:1420/）
- Rust 编译完成
- Tauri 窗口弹出
- 看到 Speekium 界面

### 方法 2: 分步启动（调试用）

```bash
# Step 1: 检查依赖
cd tauri-prototype
npm list @tauri-apps/cli

# Step 2: 如果依赖有问题，重新安装
npm install

# Step 3: 启动
npm run tauri dev
```

---

## 🎯 启动成功标志

看到以下输出表示成功：

```
    Finished `dev` profile [unoptimized + debuginfo] target(s) in X.XXs
     Running `target/debug/speekium`
```

然后会弹出一个 Tauri 窗口，显示 Speekium 界面。

---

## 🧪 测试录音功能

启动成功后：

1. **找到录音按钮**
   - 在 Tauri 窗口中找到 🎤 图标

2. **点击录音**
   - 点击按钮
   - 立即说话（3 秒内）
   - 观察界面

3. **验证修复效果**
   - ✅ 界面应该流畅，**无闪烁**
   - ✅ 录音期间界面**不应该卡住**
   - ✅ 识别结果应该准确显示
   - ✅ LLM 应该自动回复

---

## ❌ 常见问题

### 问题 1: `Cannot find module '@tauri-apps/cli-darwin-arm64'`

**解决方案**:
```bash
cd tauri-prototype
rm -rf node_modules package-lock.json
npm install
```

### 问题 2: 端口 1420 被占用

**解决方案**:
```bash
# 杀死占用端口的进程
lsof -ti:1420 | xargs kill -9

# 重新启动
npm run tauri dev
```

### 问题 3: Rust 编译失败

**检查 Rust 安装**:
```bash
rustc --version

# 如果没有安装
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

### 问题 4: Python Worker 找不到

**检查 Python 环境**:
```bash
# 确保在项目根目录
cd /Users/kww/work/opensource/speekium

# 验证 worker.py 存在
ls -lh worker.py

# 验证可执行权限
chmod +x worker.py

# 测试 Python Worker
python3 worker.py config '{}'
```

---

## 📊 预期性能

启动后，您应该体验到：

| 功能 | 性能 |
|------|------|
| 界面响应 | **流畅、无闪烁** ✅ |
| 录音操作 | **非阻塞** ✅ |
| 通信延迟 | **<1ms** ✅ |
| 启动复杂度 | **一键启动** ✅ |

---

## 🔍 验证架构修复

启动后，可以验证架构是否正确：

### 1. 检查网络请求（应该没有 HTTP）
打开浏览器开发者工具，查看 Network 选项卡，**不应该有** `http://127.0.0.1:8008` 的请求。

### 2. 检查终端输出
应该看到类似：
```
⚙️ 调用 Python worker: record
✅ 配置加载完成
```

而**不应该看到**：
```
POST http://127.0.0.1:8008/api/record
```

---

## ✅ 成功启动检查清单

启动后确认：
- [ ] Tauri 窗口已弹出
- [ ] 界面显示正常
- [ ] 没有 HTTP 请求到 8008 端口
- [ ] 点击录音按钮界面流畅
- [ ] 录音后有识别结果
- [ ] LLM 正常回复

---

## 📞 需要帮助？

如果启动失败，请提供：
1. 完整的错误信息
2. `npm run tauri dev` 的完整输出
3. 操作系统版本
4. Node.js 版本（`node -v`）
5. Rust 版本（`rustc --version`）

---

**下一步**: 执行上述步骤启动应用，然后测试录音功能！
